import 'package:flutter/material.dart';

class FlowTemp extends StatelessWidget {
  const FlowTemp({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(body: const Center(child: Text("I Am Template!")));
  }
}
