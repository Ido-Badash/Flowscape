import 'package:flutter/material.dart';

/// Frame for the child in ScapePage class
class NameFrame extends StatelessWidget {
  const NameFrame({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}