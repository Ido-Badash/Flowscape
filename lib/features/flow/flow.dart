import 'package:flutter/material.dart';

class FlowScape extends StatelessWidget {
  const FlowScape({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(body: const Center(child: Text("I Am Flowing!")));
  }
}
