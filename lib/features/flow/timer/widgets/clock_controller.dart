import 'package:flutter/material.dart';

class ClockController extends StatelessWidget {
  const ClockController({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}