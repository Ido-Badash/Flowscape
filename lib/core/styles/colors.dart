import 'package:flutter/material.dart';
class FlowColors {
  static const Color blue = Color.fromRGBO(120, 130, 255, 1.0);
  static const Color darkBlue = Color.fromRGBO(12, 12, 15, 1.0);

  static const Color lightGray = Color.fromRGBO(172, 172, 198, 1.0);

  static const Color pink = Color.fromRGBO(202, 5, 77, 1.0);

  static const Color lavender = Color.fromRGBO(226, 226, 252, 1.0);
  static const Color darkLavender = Color.fromRGBO(75, 75, 190, 0.5);

  static const Color lightBlueGray = Color.fromRGBO(239, 241, 243, 1.0);

  // cross theme
  static Color body = Color.fromRGBO(6, 6, 7, 1.0);
}
